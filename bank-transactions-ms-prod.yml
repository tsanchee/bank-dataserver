server:
  port: 28020

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:28761/eureka}
  instance:
    preferIpAddress: true

spring:
  data:
    mongodb:
      host: localhost
      port: 27019
      authentication-database: bank-transactions-db
      username: admin
      password: admin
      database: bank-transactions-db
